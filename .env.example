# DeepSeek API Configuration
DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# Existing env vars
# DATABASE_URL=postgresql://...
# DIFY_API_KEY is no longer used; all AI routes call DeepSeek.

# Research process logging (default on). Set false to disable writes.
# PROCESS_LOGGING_ENABLED=true
